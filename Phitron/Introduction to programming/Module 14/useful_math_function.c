#include<stdio.h>
#include<math.h>
int main(){
    // int ans = ceil(4.2);
    // int ans = floor(4.2);
    // int ans = round(4.78);
    // double ans = sqrt(20);
    // int ans = pow(3,2);
    int ans = abs(-13);
    printf("%d", ans);
    return 0;
}